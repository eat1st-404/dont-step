{
  "name": "dont-step",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2025-06-07 --binding BROWSER=Browser",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^4.21.0"
  }
}