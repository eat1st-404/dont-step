{
  "name": "dont-step",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2025-06-07 --binding BROWSER=Browser",
    "build:pages": "rm -rf dist && mkdir dist && cp index.html dist/index.html",
    "deploy": "wrangler pages deploy dist"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.21.0"
  }
}